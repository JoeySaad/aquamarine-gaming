# Aquamarine Gaming

A simple, Vercel-ready Next.js arcade portal for browser games.

## Run locally

```bash
npm install
npm run dev
```

## Add or update games

Edit `data/games.ts`. Each game can point to either a Unity WebGL URL or a standard iframe URL. Placeholder embed URLs are included so the pages are ready to wire up to real builds.
